<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.wenzhuo4657.dailyWeb.infrastructure.database.dao.UserDao">

    <resultMap type="cn.wenzhuo4657.dailyWeb.infrastructure.database.entity.User" id="UserMap">
        <result property="id" column="id" jdbcType="BIGINT"/>
        <result property="userId" column="user_id" jdbcType="BIGINT"/>
        <result property="oauthUserid" column="oauth_userId" jdbcType="VARCHAR"/>
        <result property="oauthProvider" column="oauth_provider" jdbcType="VARCHAR"/>
        <result property="createdAt" column="created_at" jdbcType="VARCHAR"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="avatarUrl" column="avatar_url" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="UserMap">
        select
id, user_id, oauth_userId, oauth_provider, created_at, name, avatar_url
        from user
        where id = #{id}
    </select>


    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from user
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="oauthUserid != null and oauthUserid != ''">
                and oauth_userId = #{oauthUserid}
            </if>
            <if test="oauthProvider != null and oauthProvider != ''">
                and oauth_provider = #{oauthProvider}
            </if>
            <if test="createdAt != null and createdAt != ''">
                and created_at = #{createdAt}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="avatarUrl != null and avatarUrl != ''">
                and avatar_url = #{avatarUrl}
            </if>
        </where>
    </select>
    <select id="queryByOauthId" resultType="cn.wenzhuo4657.dailyWeb.infrastructure.database.entity.User">
        select * from user where oauth_userId = #{oauthProviderUserId} and oauth_provider = #{oauthProvider}
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into user(user_id, oauth_userId, oauth_provider, created_at, name, avatar_url)
        values (#{userId}, #{oauthUserid}, #{oauthProvider}, #{createdAt}, #{name}, #{avatarUrl})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into user(user_id, oauth_userId, oauth_provider, created_at, name, avatar_url)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.userId}, #{entity.oauthUserid}, #{entity.oauthProvider}, #{entity.createdAt}, #{entity.name}, #{entity.avatarUrl})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into user(user_id, oauth_userId, oauth_provider, created_at, name, avatar_url)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userId}, #{entity.oauthUserid}, #{entity.oauthProvider}, #{entity.createdAt}, #{entity.name}, #{entity.avatarUrl})
        </foreach>
        on duplicate key update
user_id = values(user_id),
oauth_userId = values(oauth_userId),
oauth_provider = values(oauth_provider),
created_at = values(created_at),
name = values(name),
avatar_url = values(avatar_url)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update user
        <set>
            <if test="userId != null">
                user_id = #{userId},
            </if>
            <if test="oauthUserid != null and oauthUserid != ''">
                oauth_userId = #{oauthUserid},
            </if>
            <if test="oauthProvider != null and oauthProvider != ''">
                oauth_provider = #{oauthProvider},
            </if>
            <if test="createdAt != null and createdAt != ''">
                created_at = #{createdAt},
            </if>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="avatarUrl != null and avatarUrl != ''">
                avatar_url = #{avatarUrl},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from user where id = #{id}
    </delete>

</mapper>

